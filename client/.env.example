# Copy to .env (local dev) or .env.production (build) and set your API URL.
# Leave blank/unset for local dev to use the Vite proxy (/api -> localhost:5000).
VITE_API_URL=https://travel-erp-api.vercel.app
